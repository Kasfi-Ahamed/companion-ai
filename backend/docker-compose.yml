version: '3.8'

services:
  backend:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - mongodb
    environment:
      - MONGO_URI=mongodb://mongodb:27017/companion-ai
      - JWT_SECRET=testsecret123

  mongodb:
    image: mongo
    ports:
      - "27017:27017"
